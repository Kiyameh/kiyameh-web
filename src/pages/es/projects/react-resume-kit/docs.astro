---
import HtmlLayout from '@/layouts/HtmlLayout.astro'
import NavLayout from '@/layouts/NavLayout.astro'
import Breadcrumb from '@/components/ui/Breadcrumb/Breadcrumb.tsx'
import { SiGithub, SiNpm, SiVercel } from '@icons-pack/react-simple-icons'
import { Circle, Laptop } from 'lucide-react'

/* 
  @version 3
  @revisado 15/09/2025
  @descripcion: Página de documentación completa de React Resume Kit en español
*/

const breadcrumbItems = [
  { label: 'Inicio', href: '/es' },
  { label: 'Proyectos', href: '/es/projects' },
  { label: 'React Resume Kit', href: '/es/projects/react-resume-kit' },
  { label: 'Documentación' }
]
---

<HtmlLayout pageTitle="React Resume Kit - Documentación">
  <NavLayout>
    <div class="docs-container">
      <Breadcrumb items={breadcrumbItems} client:load />
      <header class="docs-header">
        <div class="logo-section">
          <img src="/projects/reactresumekit/logo.png" alt="React Resume Kit Logo" class="logo" />
          <div class="header-text">
      <h1>React Resume Kit</h1>
            <p class="subtitle">Componente React moderno y personalizable para crear currículums profesionales</p>
          </div>
        </div>
        <div class="badges">
          <a href="https://www.npmjs.com/package/react-resume-kit" target="_blank" class="badge-button npm-badge">
            <SiNpm/>
            NPM</a>
            <a href="https://github.com/Kiyameh/react-resume-kit" target="_blank" class="badge-button github-badge">
              <SiGithub/>
              GitHub</a>
              <a href="https://react-resume-kit.vercel.app/" target="_blank" class="badge-button live-badge">
                <Laptop />
                Live demo</a>
        </div>
      </header>

      <nav class="table-of-contents">
        <h2>Contenido</h2>
        <ul>
          <li><a href="#caracteristicas">Características</a></li>
          <li><a href="#instalacion">Instalación</a></li>
          <li><a href="#inicio-rapido">Inicio Rápido</a></li>
          <li><a href="#configuracion">Configuración</a></li>
          <li><a href="#estructura-contenido">Estructura del Contenido</a></li>
          <li><a href="#iconos">Iconos</a></li>
          <li><a href="#idiomas">Soporte de Idiomas</a></li>
          <li><a href="#personalizacion">Personalización</a></li>
          <li><a href="#toolbar-personalizado">Toolbar Personalizado</a></li>
        </ul>
      </nav>

      <main class="docs-content">
        <section id="caracteristicas" class="docs-section">
          <h2>Características</h2>
          <div class="features-grid">
            <div class="feature-card">
              <span class="feature-icon">🌐</span>
              <h3>Soporte multiidioma</h3>
              <p>Fácil configuración para múltiples idiomas</p>
            </div>
            <div class="feature-card">
              <span class="feature-icon">📄</span>
              <h3>Descarga PDF</h3>
              <p>Funcionalidad integrada de descarga en PDF</p>
            </div>
            <div class="feature-card">
              <span class="feature-icon">🎨</span>
              <h3>Temas personalizables</h3>
              <p>Variables CSS para personalización completa</p>
            </div>
            <div class="feature-card">
              <span class="feature-icon">🎯</span>
              <h3>Soporte de iconos</h3>
              <p>Compatible con cualquier librería de iconos React</p>
            </div>
            <div class="feature-card">
              <span class="feature-icon">⚡</span>
              <h3>Fácil integración</h3>
              <p>Configuración simple y rápida</p>
            </div>
            <div class="feature-card">
              <span class="feature-icon">📱</span>
              <h3>Diseño responsivo</h3>
              <p>Se adapta a cualquier dispositivo</p>
            </div>
          </div>
        </section>

        <section id="instalacion" class="docs-section">
          <h2>Instalación</h2>
          <div class="code-block">
            <pre><code>npm install react-resume-kit</code></pre>
          </div>
        </section>

        <section id="inicio-rapido" class="docs-section">
          <h2>Inicio Rápido</h2>
          <div class="code-block">
            <pre><code>import &#123;ResumeLayout, ResumeHeader&#125; from 'react-resume-kit'
import &#123;content&#125; from './tu-archivo-de-contenido'

function App() &#123;
  return (
    &lt;ResumeLayout resumeContent=&#123;content&#125;&gt;
      &lt;ResumeHeader /&gt;
    &lt;/ResumeLayout&gt;
  )
&#125;</code></pre>
          </div>
        </section>

        <section id="configuracion" class="docs-section">
          <h2>Configuración</h2>
          
          <h3>Paso 1: Componentes disponibles</h3>
          <p>Agrega el componente <code>ResumeLayout</code> envolviendo uno o más de los siguientes componentes:</p>
          <div class="code-block">
            <pre><code>&lt;ResumeHeader/&gt;
&lt;ResumeAbout/&gt;
&lt;ResumeWorks/&gt;
&lt;ResumeCourses/&gt;
&lt;ResumeTechnologies/&gt;
&lt;ResumeSoftSkills/&gt;
&lt;ResumeProjects/&gt;
&lt;ResumeContact/&gt;</code></pre>
          </div>

          <h3>Paso 2: Props del ResumeLayout</h3>
          <div class="props-table">
            <table>
              <thead>
                <tr>
                  <th>Prop</th>
                  <th>Tipo</th>
                  <th>Descripción</th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td><code>initialLanguage</code></td>
                  <td>string</td>
                  <td>Idioma inicial (ej: "es", "en")</td>
                </tr>
                <tr>
                  <td><code>resumeContent</code></td>
                  <td>ResumeContent</td>
                  <td>Objeto con todo el contenido del currículum</td>
                </tr>
                <tr>
                  <td><code>enableLanguageSwitch</code></td>
                  <td>boolean</td>
                  <td>Habilita el selector de idioma</td>
                </tr>
                <tr>
                  <td><code>enablePdfDownload</code></td>
                  <td>boolean</td>
                  <td>Habilita la descarga en PDF</td>
                </tr>
              </tbody>
            </table>
          </div>
        </section>

        <section id="estructura-contenido" class="docs-section">
          <h2>Estructura del Contenido</h2>
          <p>El contenido debe seguir esta estructura TypeScript:</p>
          <div class="code-block">
            <pre><code>import &#123;ReactElement&#125; from 'react'

type ResumeContent = &#123;
  // Uno para cada idioma
  en: &#123;
    // Nombres de secciones
    about_title: string
    works_title: string
    courses_title: string
    techs_title: string
    soft_skills_title: string
    soft_skills_subtitle: string
    projects_title: string
    projects_subtitle: string
    connect_title: string

    // Texto del botón de descarga
    download_pdf: string

    // Selector de idioma
    switcher_text: string
    language_labels: Array&lt;&#123;label: string; value: string&#125;&gt;

    // Header
    name: string
    title: string
    email: string
    phone: string
    location: string
    picture?: string

    // Acerca de
    about_text: string

    // Experiencia
    works: Array&lt;&#123;
      title: string
      company: string
      date: string
      location: string
      points: string[]
    &#125;&gt;

    // Cursos
    courses: Array&lt;&#123;
      degree: string
      school: string
      date: string
    &#125;&gt;

    // Tecnologías
    technologies: Array&lt;&#123;
      name: string
      icon: ReactElement
    &#125;&gt;

    // Habilidades Blandas
    soft_skills: Array&lt;&#123;
      title: string
      description: string
      icon: ReactElement
    &#125;&gt;

    // Proyectos
    projects: Array&lt;&#123;
      title: string
      description: string
      features: string
      technologies: string[]
      link?: string
    &#125;&gt;

    // Footer
    author: string
    socialLinks: Array&lt;&#123;
      name: string
      icon: ReactElement
      url: string
    &#125;&gt;
  &#125;
&#125;</code></pre>
          </div>
        </section>

        <section id="iconos" class="docs-section">
          <h2>Iconos</h2>
          <p>El componente soporta cualquier elemento React como icono. Puedes usar iconos de cualquier librería:</p>
          
          <h3>Ejemplo con Simple Icons</h3>
          <div class="code-block">
            <pre><code>import &#123;SiReact, SiTypescript&#125; from '@icons-pack/react-simple-icons'

const content = &#123;
  en: &#123;
    // ...
    technologies: [
      &#123;name: 'React', icon: &lt;SiReact /&gt;&#125;,
      &#123;name: 'TypeScript', icon: &lt;SiTypescript /&gt;&#125;,
      // ...
    ],
    // ...
  &#125;,
&#125;</code></pre>
          </div>

          <div class="tip-box">
            <strong>💡 Tip:</strong> Eres responsable de importar y renderizar el icono que quieras. El componente automáticamente inyectará la clase CSS necesaria para un estilo consistente.
          </div>
        </section>

        <section id="idiomas" class="docs-section">
          <h2>Soporte de Idiomas</h2>
          <p>El componente soporta múltiples idiomas. Proporciona el contenido para cada idioma:</p>
          <div class="code-block">
            <pre><code>const content = &#123;
  en: &#123;
    /* Contenido en inglés */
  &#125;,
  es: &#123;
    /* Contenido en español */
  &#125;,
&#125;</code></pre>
          </div>
        </section>

        <section id="personalizacion" class="docs-section">
          <h2>Personalización</h2>
          
          <h3>Sistema de Estilos</h3>
          <p>Todos los componentes usan clases que comienzan con <code>.rrk-</code> para fácil identificación y personalización.</p>

          <h3>Personalización Rápida</h3>
          <div class="code-block">
            <pre><code>:root &#123;
  --rrk-primary: #3b82f6; /* Tu color principal */
  --rrk-surface-100: #ffffff; /* Color de fondo */
  --rrk-content: #1f2937; /* Color de texto */
  --rrk-radius-medium: 0.75rem; /* Radio de borde */
&#125;</code></pre>
          </div>

          <h3>Personalización Avanzada</h3>
          <ul>
            <li><strong>Variables CSS:</strong> Sobrescribe cualquier variable CSS para cambiar colores, sombras, bordes, etc.</li>
            <li><strong>Clases de Componentes:</strong> Dirige componentes específicos usando sus clases <code>.rrk-</code></li>
            <li><strong>Herramientas de Desarrollador:</strong> Inspecciona elementos para ver todas las clases disponibles</li>
            <li><strong>Repositorio:</strong> Revisa los archivos CSS de los componentes para referencia completa</li>
          </ul>
        </section>

        <section id="toolbar-personalizado" class="docs-section">
          <h2>Toolbar Personalizado</h2>
          <p>Crea tu propio toolbar usando el hook <code>useToolbar</code>:</p>
          <div class="code-block">
            <pre><code>import &#123;useToolbar&#125; from 'react-resume-kit'

function MyCustomToolbar() &#123;
  const &#123;handlePrint, languageLabels, handleLanguageSelect&#125; = useToolbar()

  return (
    &lt;div className="my-toolbar"&gt;
      &lt;button onClick=&#123;handlePrint&#125;&gt;📄 Descargar&lt;/button&gt;
      &lt;select onChange=&#123;(e) =&gt; handleLanguageSelect(e.target.value)&#125;&gt;
        &#123;languageLabels.map((lang) =&gt; (
          &lt;option key=&#123;lang.value&#125; value=&#123;lang.value&#125;&gt;
            &#123;lang.label&#125;
          &lt;/option&gt;
        ))&#125;
      &lt;/select&gt;
    &lt;/div&gt;
  )
&#125;</code></pre>
          </div>
        </section>

        <section class="docs-section">
          <h2>Enlaces Útiles</h2>
          <div class="links-grid">
            <a href="https://react-resume-kit.vercel.app/" target="_blank" class="link-card">
              <h3>🌐 Demo en Vivo</h3>
              <p>Explora todas las características en acción</p>
            </a>
            <a href="https://www.npmjs.com/package/react-resume-kit" target="_blank" class="link-card">
              <h3>📦 NPM Package</h3>
              <p>Descarga e instala desde NPM</p>
            </a>
            <a href="https://github.com/kiyameh/react-resume-kit" target="_blank" class="link-card">
              <h3>💻 Repositorio</h3>
              <p>Código fuente y contribuciones</p>
            </a>
          </div>
        </section>

        <footer class="docs-footer">
          <p>Creado por <strong>Andoni Abarzuza (@kiyameh)</strong></p>
          <p>Licencia MIT - ¡Las contribuciones son bienvenidas!</p>
        </footer>
      </main>
    </div>
  </NavLayout>
</HtmlLayout>

<style>
  .docs-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem;
    background-color: var(--surface-200);
    min-height: 100vh;
  }

  .docs-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 3rem;
    padding-bottom: 2rem;
    border-bottom: 2px solid var(--primary);
    flex-wrap: wrap;
    gap: 2rem;
  }

  .logo-section {
    display: flex;
    align-items: center;
    gap: 2rem;
  }

  .logo {
    width: 80px;
    height: 80px;
    border-radius: var(--radius-medium);
  }

  .header-text h1 {
    font-size: 2.5rem;
    color: var(--content);
    margin: 0;
  }

  .subtitle {
    color: var(--content-200);
    font-size: 1.1rem;
    margin: 0.5rem 0 0 0;
  }

  .badges {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .badge-button {
    padding: 0.5rem 1rem;
    background: var(--surface-300);
    border-radius: 99px;
    border: 2px solid transparent;
    text-decoration: none;
    font-weight: 600;
    cursor: pointer;
    display: flex;
    gap: 0.5rem;
    width: fit-content;
    text-align: center;
    transition: all 0.2s ease;
  }

  .npm-badge {
    color: orangered;
  }
  .npm-badge:hover {
    border: 2px solid orangered;
  }

  .github-badge {
    color: var(--primary);
  }
  .github-badge:hover {
    border: 2px solid var(--primary);
  }
  .live-badge {
    color: var(--content);
  }
  .live-badge:hover {
    border: 2px solid var(--content);
  }

  .table-of-contents {
    background-color: var(--surface-100);
    padding: 1.5rem;
    border-radius: var(--radius-medium);
    margin-bottom: 3rem;
  }

  .table-of-contents h2 {
    margin-top: 0;
    color: var(--content);
  }

  .table-of-contents ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 0.5rem;
  }

  .table-of-contents a {
    color: var(--primary);
    text-decoration: none;
    padding: 0.5rem;
    border-radius: var(--radius-small);
    transition: background-color 0.2s ease;
  }

  .table-of-contents a:hover {
    background-color: var(--surface-200);
    transform: translateX(4px);
  }

  .docs-content {
    line-height: 1.6;
  }

  .docs-section {
    margin-bottom: 4rem;
  }

  .docs-section h2 {
    color: var(--content);
    font-size: 2rem;
    margin-bottom: 1.5rem;
    border-bottom: 2px solid var(--primary);
    padding-bottom: 0.5rem;
  }

  .docs-section h3 {
    color: var(--content);
    font-size: 1.5rem;
    margin: 2rem 0 1rem 0;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 1.5rem;
    margin: 2rem 0;
  }

  .feature-card {
    background-color: var(--surface-100);
    padding: 1.5rem;
    border-radius: var(--radius-medium);
    text-align: center;
    transition: all 0.3s ease;
  }

  .feature-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
    background-color: var(--surface-200);
  }

  .feature-icon {
    font-size: 2.5rem;
    display: block;
    margin-bottom: 1rem;
  }

  .feature-card h3 {
    margin: 0 0 0.5rem 0;
    color: var(--content);
  }

  .feature-card p {
    margin: 0;
    color: var(--content-200);
  }

  .code-block {
    background-color: var(--surface-100);
    border-radius: var(--radius-medium);
    padding: 1.5rem;
    margin: 1.5rem 0;
    overflow-x: auto;
  }

  .code-block pre {
    margin: 0;
    white-space: pre-wrap;
  }

  .code-block code {
    font-family: var(--font-mono);
    font-size: 0.9rem;
    color: var(--content);
  }

  .props-table {
    margin: 1.5rem 0;
    overflow-x: auto;
  }

  .props-table table {
    width: 100%;
    border-collapse: collapse;
    background-color: var(--surface-100);
    border-radius: var(--radius-medium);
    overflow: hidden;
  }

  .props-table th,
  .props-table td {
    padding: 1rem;
    text-align: left;
    border-bottom: 1px solid var(--surface-200);
  }

  .props-table th {
    background-color: var(--surface-300);
    font-weight: 600;
    color: var(--content);
  }

  .props-table code {
    background-color: var(--surface-300);
    padding: 0.2rem 0.4rem;
    border-radius: var(--radius-small);
    font-size: 0.9rem;
  }

  .tip-box {
    background-color: var(--accent-100);
    border-left: 4px solid var(--accent);
    padding: 1rem;
    margin: 1.5rem 0;
    border-radius: var(--radius-small);
  }

  .links-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
    margin: 2rem 0;
  }

  .link-card {
    background-color: var(--surface-100);
    padding: 1.5rem;
    border-radius: var(--radius-medium);
    text-decoration: none;
    color: var(--content);
    transition: all 0.3s ease;
  }

  .link-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
    background-color: var(--surface-200);
  }

  .link-card h3 {
    margin: 0 0 0.5rem 0;
    color: var(--primary);
  }

  .link-card p {
    margin: 0;
    color: var(--content);
  }

  .docs-footer {
    text-align: center;
    padding: 2rem;
    border-top: 2px solid var(--surface-200);
    margin-top: 4rem;
    margin-bottom: 4rem;
    color: var(--content);
    background-color: var(--surface-200);
    border-radius: var(--radius-medium);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .docs-container {
      padding: 1rem;
    }

    .docs-header {
      flex-direction: column;
      text-align: center;
    }

    .logo-section {
      flex-direction: column;
      text-align: center;
    }

    .header-text h1 {
      font-size: 2rem;
    }

    .table-of-contents ul {
      grid-template-columns: 1fr;
    }

    .features-grid,
    .links-grid {
      grid-template-columns: 1fr;
    }
  }

  /* Smooth scrolling for anchor links */
  html {
    scroll-behavior: smooth;
  }

  /* Highlight active section */
  .docs-section:target {
    scroll-margin-top: 2rem;
  }
</style>
