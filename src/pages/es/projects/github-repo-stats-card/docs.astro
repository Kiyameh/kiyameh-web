---
import HtmlLayout from '@/layouts/HtmlLayout.astro'
import NavLayout from '@/layouts/NavLayout.astro'
import Breadcrumb from '@/components/ui/Breadcrumb/Breadcrumb.tsx'
import { SiGithub, SiNpm } from '@icons-pack/react-simple-icons'

/* 
  @version 3
  @revisado 15/09/2025
  @descripcion: Página de documentación completa de GitHub Repo Stats Card en español
*/

const breadcrumbItems = [
  { label: 'Inicio', href: '/es' },
  { label: 'Proyectos', href: '/es/projects' },
  { label: 'GitHub Repo Stats Card', href: '/es/projects/github-repo-stats-card' },
  { label: 'Documentación' }
]
---

<HtmlLayout pageTitle="GitHub Repo Stats Card - Documentación">
  <NavLayout>
    <div class="docs-container">
      <Breadcrumb items={breadcrumbItems} client:load />
      <header class="docs-header">
        <div class="logo-section">
          <img src="/projects/githubrepostatscard/logo.svg" alt="GitHub Repo Stats Card Logo" class="logo" />
          <div class="header-text">
      <h1>GitHub Repo Stats Card</h1>
            <p class="subtitle">Paquete NPM ligero y sin dependencias para mostrar estadísticas de repositorios de GitHub</p>
          </div>
        </div>
        <div class="badges">
            <a href="https://www.npmjs.com/package/github-repo-stats-card" class="badge-button npm-badge">
              <SiNpm/>
              NPM</a>
            <a href="https://github.com/Kiyameh/repo-stats-card" class="badge-button github-badge">
              <SiGithub/>
              GitHub</a>
        </div>
      </header>

      <nav class="table-of-contents">
        <h2>Contenido</h2>
        <ul>
          <li><a href="#caracteristicas">Características</a></li>
          <li><a href="#instalacion">Instalación</a></li>
          <li><a href="#uso-basico">Uso Básico</a></li>
          <li><a href="#api">API Reference</a></li>
          <li><a href="#estadisticas">Estadísticas Mostradas</a></li>
          <li><a href="#accesibilidad">Accesibilidad</a></li>
          <li><a href="#personalizacion">Personalización</a></li>
          <li><a href="#ejemplos">Ejemplos Avanzados</a></li>
        </ul>
      </nav>

      <main class="docs-content">
        <section id="caracteristicas" class="docs-section">
          <h2>Características</h2>
          <div class="features-grid">
            <div class="feature-card">
              <span class="feature-icon">🚀</span>
              <h3>Sin dependencias</h3>
              <p>Funciona únicamente con APIs nativas del navegador</p>
            </div>
            <div class="feature-card">
              <span class="feature-icon">🎨</span>
              <h3>Estilo autocontenido</h3>
              <p>CSS inyectado automáticamente, no interfiere con estilos existentes</p>
            </div>
            <div class="feature-card">
              <span class="feature-icon">📱</span>
              <h3>Responsivo</h3>
              <p>Se adapta a diferentes tamaños de pantalla con layout adaptativo</p>
            </div>
            <div class="feature-card">
              <span class="feature-icon">⚡</span>
              <h3>Ligero</h3>
              <p>Solo JavaScript vanilla con ES Modules</p>
            </div>
            <div class="feature-card">
              <span class="feature-icon">🛡️</span>
              <h3>Manejo de errores</h3>
              <p>Muestra mensajes de error elegantes cuando algo falla</p>
            </div>
            <div class="feature-card">
              <span class="feature-icon">🔐</span>
              <h3>Autenticación opcional</h3>
              <p>Soporte para token de GitHub para aumentar límites de API</p>
            </div>
            <div class="feature-card">
              <span class="feature-icon">💻</span>
              <h3>Lenguajes de programación</h3>
              <p>Top 5 lenguajes más utilizados en el repositorio</p>
            </div>
            <div class="feature-card">
              <span class="feature-icon">♿</span>
              <h3>Accesible</h3>
              <p>Cumple con estándares WCAG y incluye soporte para lectores de pantalla</p>
            </div>
            <div class="feature-card">
              <span class="feature-icon">🌙</span>
              <h3>Tema oscuro</h3>
              <p>Soporte automático para prefers-color-scheme: dark</p>
            </div>
            <div class="feature-card">
              <span class="feature-icon">📊</span>
              <h3>Gráficos duales</h3>
              <p>Gráfico de torta en desktop, gráfico lineal en móvil</p>
            </div>
          </div>
        </section>

        <section id="instalacion" class="docs-section">
          <h2>Instalación</h2>
          <div class="code-block">
            <pre><code>npm install github-repo-stats-card</code></pre>
          </div>
        </section>

        <section id="uso-basico" class="docs-section">
          <h2>Uso Básico</h2>
          
          <h3>HTML</h3>
          <div class="code-block">
            <pre><code>&lt;div id="github-card"&gt;&lt;/div&gt;</code></pre>
          </div>

          <h3>JavaScript</h3>
          <div class="code-block">
            <pre><code>import &#123;getRepoStats, renderRepoCard&#125; from 'github-repo-stats-card'

// Obtener datos del repositorio
const stats = await getRepoStats('vuejs/vue')

// Renderizar la tarjeta con los datos obtenidos
renderRepoCard('#github-card', stats)

// Con token de autenticación
const statsWithToken = await getRepoStats('vuejs/vue', 'ghp_your_token_here')
renderRepoCard('#github-card', statsWithToken)</code></pre>
          </div>

          <h3>TypeScript</h3>
          <p>El paquete incluye soporte completo para TypeScript con tipos definidos:</p>
          <div class="code-block">
            <pre><code>import &#123;getRepoStats, renderRepoCard, RepoStats&#125; from 'github-repo-stats-card'

// Obtener datos tipados
const stats: RepoStats = await getRepoStats('vuejs/vue')
console.log(stats.stargazersCount) // number
console.log(stats.languages) // Record&lt;string, number&gt;

// Renderizar con datos tipados
renderRepoCard('#github-card', stats)</code></pre>
          </div>

          <div class="warning-box">
            <h4>⚠️ Importante sobre Seguridad</h4>
            <p><strong>getRepoStats</strong> debe utilizarse siempre en el lado del servidor para evitar exponer el token al cliente.</p>
            <p><strong>renderRepoCard</strong> puede emplearse con seguridad en el cliente o en el servidor.</p>
          </div>
        </section>

        <section id="api" class="docs-section">
          <h2>API Reference</h2>
          
          <h3>getRepoStats(repoName, githubAuthToken?)</h3>
          <p>Obtiene solo los datos del repositorio sin manipular el DOM.</p>
          
          <div class="api-table">
            <table>
              <thead>
                <tr>
                  <th>Parámetro</th>
                  <th>Tipo</th>
                  <th>Requerido</th>
                  <th>Descripción</th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td><code>repoName</code></td>
                  <td>string</td>
                  <td>Sí</td>
                  <td>Nombre del repositorio en formato 'usuario/nombre-del-repo'</td>
                </tr>
                <tr>
                  <td><code>githubAuthToken</code></td>
                  <td>string</td>
                  <td>No</td>
                  <td>Token de autenticación de GitHub para aumentar límites de API</td>
                </tr>
              </tbody>
            </table>
          </div>
          
          <p><strong>Retorna:</strong> <code>Promise&lt;RepoStats&gt;</code> - Estadísticas del repositorio</p>
          
          <div class="warning-box">
            <p><strong>⚠️ Importante:</strong> Esta función debe utilizarse siempre en el lado del servidor para evitar exponer el token al cliente.</p>
          </div>

          <h3>renderRepoCard(selector, stats)</h3>
          <p>Renderiza una tarjeta de estadísticas de GitHub con datos proporcionados.</p>
          
          <div class="api-table">
            <table>
              <thead>
                <tr>
                  <th>Parámetro</th>
                  <th>Tipo</th>
                  <th>Requerido</th>
                  <th>Descripción</th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td><code>selector</code></td>
                  <td>string</td>
                  <td>Sí</td>
                  <td>Selector CSS del elemento contenedor donde se mostrará la tarjeta</td>
                </tr>
                <tr>
                  <td><code>stats</code></td>
                  <td>RepoStats</td>
                  <td>Sí</td>
                  <td>Estadísticas del repositorio obtenidas con getRepoStats</td>
                </tr>
              </tbody>
            </table>
          </div>
          
          <p><strong>Retorna:</strong> <code>void</code></p>
          
          <div class="info-box">
            <p><strong>ℹ️ Nota:</strong> Esta función puede emplearse con seguridad en el cliente o en el servidor.</p>
          </div>
        </section>

        <section id="estadisticas" class="docs-section">
          <h2>Estadísticas Mostradas</h2>
          <p>La tarjeta muestra las siguientes estadísticas del repositorio:</p>
          
          <div class="stats-grid">
            <div class="stat-item">
              <span class="stat-icon">⭐</span>
              <div>
                <h4>Estrellas (Stargazers)</h4>
                <p>Número de usuarios que han marcado el repositorio como favorito</p>
              </div>
            </div>
            <div class="stat-item">
              <span class="stat-icon">🍴</span>
              <div>
                <h4>Forks</h4>
                <p>Número de copias del repositorio creadas por otros usuarios</p>
              </div>
            </div>
            <div class="stat-item">
              <span class="stat-icon">❗</span>
              <div>
                <h4>Issues abiertos</h4>
                <p>Número de problemas o solicitudes de características pendientes</p>
              </div>
            </div>
            <div class="stat-item">
              <span class="stat-icon">📝</span>
              <div>
                <h4>Descripción</h4>
                <p>Descripción del repositorio proporcionada por el autor</p>
              </div>
            </div>
            <div class="stat-item">
              <span class="stat-icon">📅</span>
              <div>
                <h4>Fecha de creación</h4>
                <p>Cuándo fue creado el repositorio</p>
              </div>
            </div>
            <div class="stat-item">
              <span class="stat-icon">🔄</span>
              <div>
                <h4>Última actualización</h4>
                <p>Fecha del último commit o cambio en el repositorio</p>
              </div>
            </div>
            <div class="stat-item">
              <span class="stat-icon">💻</span>
              <div>
                <h4>Top 5 lenguajes</h4>
                <p>Los 5 lenguajes de programación más utilizados con porcentajes</p>
              </div>
            </div>
            <div class="stat-item">
              <span class="stat-icon">🔗</span>
              <div>
                <h4>Enlace directo</h4>
                <p>Link que lleva directamente al repositorio en GitHub</p>
              </div>
            </div>
          </div>
        </section>

        <section id="accesibilidad" class="docs-section">
          <h2>Accesibilidad y Diseño</h2>
          
          <h3>Características de Accesibilidad</h3>
          <ul>
            <li><strong>Roles ARIA:</strong> Uso apropiado de roles como <code>main</code>, <code>group</code>, <code>img</code> y <code>section</code></li>
            <li><strong>Etiquetas descriptivas:</strong> <code>aria-label</code> y <code>aria-labelledby</code> para elementos interactivos</li>
            <li><strong>Texto para lectores de pantalla:</strong> Contenido oculto pero accesible para tecnologías asistivas</li>
            <li><strong>Navegación por teclado:</strong> Enlaces con <code>target="_blank"</code> y <code>rel="noopener noreferrer"</code></li>
            <li><strong>Contraste de colores:</strong> Cumple con estándares de accesibilidad WCAG</li>
          </ul>

          <h3>Diseño Responsivo</h3>
          <ul>
            <li><strong>Layout adaptativo:</strong> Grid en desktop, flexbox en móvil</li>
            <li><strong>Gráficos duales:</strong> Gráfico de torta en desktop, gráfico lineal en móvil</li>
            <li><strong>Tipografía escalable:</strong> Variables CSS para tamaños de fuente consistentes</li>
            <li><strong>Espaciado responsivo:</strong> Márgenes y padding que se adaptan al tamaño de pantalla</li>
            <li><strong>Tema oscuro:</strong> Soporte automático para <code>prefers-color-scheme: dark</code></li>
          </ul>
        </section>

        <section id="personalizacion" class="docs-section">
          <h2>Personalización</h2>
          <p>El paquete inyecta automáticamente los estilos CSS necesarios. Los estilos están encapsulados en clases con prefijo <code>stat-card-</code> para evitar conflictos.</p>
          
          <h3>Variables CSS Disponibles</h3>
          <p>Puedes sobrescribir estas variables CSS para personalizar la apariencia:</p>
          <div class="code-block">
            <pre><code>:root &#123;
  /* Colores */
  --stat-card-surface: #ffffff;
  --stat-card-border: #e1e5e9;
  --stat-card-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  --stat-card-text-primary: #24292f;
  --stat-card-text-secondary: #656d76;
  --stat-card-text-muted: #8b949e;
  --stat-card-accent: #0969da;
  --stat-card-accent-hover: #0860ca;
  
  /* Espaciado */
  --stat-card-spacing-xs: 4px;
  --stat-card-spacing-sm: 8px;
  --stat-card-spacing-md: 16px;
  --stat-card-spacing-lg: 24px;
  
  /* Tipografía */
  --stat-card-font-sm: 0.875rem;
  --stat-card-font-base: 1rem;
  --stat-card-font-lg: 1.125rem;
  
  /* Bordes */
  --stat-card-radius: 12px;
&#125;</code></pre>
          </div>

          <h3>Tema Oscuro Personalizado</h3>
          <div class="code-block">
            <pre><code>@media (prefers-color-scheme: dark) &#123;
  :root &#123;
    --stat-card-surface: #0d1117;
    --stat-card-border: #30363d;
    --stat-card-text-primary: #f0f6fc;
    --stat-card-text-secondary: #8b949e;
    --stat-card-text-muted: #6e7681;
    --stat-card-accent: #58a6ff;
    --stat-card-accent-hover: #4493f8;
  &#125;
&#125;</code></pre>
          </div>
        </section>

        <section id="ejemplos" class="docs-section">
          <h2>Ejemplos Avanzados</h2>
          
          <h3>Con Manejo de Errores</h3>
          <div class="code-block">
            <pre><code>import &#123;getRepoStats, renderRepoCard&#125; from 'github-repo-stats-card'

async function loadGitHubCard() &#123;
  try &#123;
    const stats = await getRepoStats('usuario/repositorio')
    renderRepoCard('#my-card', stats)
  &#125; catch (error) &#123;
    console.error('Error al cargar la tarjeta:', error)
    document.querySelector('#my-card').innerHTML = 
      '&lt;p&gt;Error al cargar las estadísticas del repositorio&lt;/p&gt;'
  &#125;
&#125;

loadGitHubCard()</code></pre>
          </div>

          <h3>Múltiples Tarjetas</h3>
          <div class="code-block">
            <pre><code>const repositories = ['vuejs/vue', 'facebook/react', 'angular/angular']

async function loadMultipleCards() &#123;
  for (const [index, repo] of repositories.entries()) &#123;
    try &#123;
      const stats = await getRepoStats(repo)
      renderRepoCard(`#card-$&#123;index&#125;`, stats)
    &#125; catch (error) &#123;
      console.error(`Error loading $&#123;repo&#125;:`, error)
    &#125;
  &#125;
&#125;</code></pre>
          </div>

          <h3>Con Variables de Entorno</h3>
          <div class="code-block">
            <pre><code>// En Node.js con variables de entorno
const GITHUB_TOKEN = process.env.GITHUB_TOKEN

async function serverSideRender(repoName) &#123;
  const stats = await getRepoStats(repoName, GITHUB_TOKEN)
  return stats
&#125;

// En el cliente
const stats = await fetch('/api/github-stats?repo=vuejs/vue')
  .then(res =&gt; res.json())
renderRepoCard('#github-card', stats)</code></pre>
          </div>

          <h3>Límites de la API de GitHub</h3>
          <div class="info-box">
            <h4>📝 Límites de la API de GitHub</h4>
            <ul>
              <li><strong>Sin autenticación:</strong> 60 requests por hora por IP</li>
              <li><strong>Con token:</strong> 5,000 requests por hora</li>
              <li><strong>Recomendación:</strong> Usa un token para proyectos en producción</li>
            </ul>
          </div>

          <h3>Crear un Token de GitHub</h3>
          <ol>
            <li>Ve a <a href="https://github.com/settings/tokens" target="_blank">GitHub Settings → Developer settings → Personal access tokens</a></li>
            <li>Genera un nuevo token con permisos <code>public_repo</code></li>
            <li>Usa el token como segundo parámetro en <code>getRepoStats</code></li>
          </ol>
        </section>

        <section class="docs-section">
          <h2>Enlaces Útiles</h2>
          <div class="links-grid">
            <a href="https://www.npmjs.com/package/github-repo-stats-card" target="_blank" class="link-card">
              <h3>📦 NPM Package</h3>
              <p>Instala el paquete desde NPM</p>
            </a>
            <a href="https://github.com/Kiyameh/repo-stats-card" target="_blank" class="link-card">
              <h3>💻 Repositorio</h3>
              <p>Código fuente y ejemplos</p>
            </a>
            <a href="https://docs.github.com/en/rest/reference/repos" target="_blank" class="link-card">
              <h3>🔗 GitHub API</h3>
              <p>Documentación de la API de GitHub</p>
            </a>
          </div>
        </section>

        <footer class="docs-footer">
          <p>Creado por <strong>Andoni Abarzuza (@kiyameh)</strong></p>
          <p>Licencia MIT - ¡Las contribuciones son bienvenidas!</p>
        </footer>
      </main>
    </div>
  </NavLayout>
</HtmlLayout>

<style>
  .docs-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem;
    background-color: var(--surface-200);
    min-height: 100vh;
  }

  .docs-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 3rem;
    padding-bottom: 2rem;
    border-bottom: 2px solid var(--primary);
    flex-wrap: wrap;
    gap: 2rem;
  }

  .logo-section {
    display: flex;
    align-items: center;
    gap: 2rem;
  }

  .logo {
    width: 80px;
    height: 80px;
    border-radius: var(--radius-medium);
  }

  .header-text h1 {
    font-size: 2.5rem;
    color: var(--content);
    margin: 0;
  }

  .subtitle {
    font-size: 1.1rem;
    margin: 0.5rem 0 0 0;
  }

  .badges {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .badge-button {
    padding: 0.5rem 1rem;
    background: var(--surface-300);
    border-radius: 99px;
    border: 2px solid transparent;
    text-decoration: none;
    font-weight: 600;
    cursor: pointer;
    display: flex;
    gap: 0.5rem;
    width: fit-content;
    text-align: center;
    transition: all 0.2s ease;
  }
  .npm-badge {
    color: orangered;
  }
  .npm-badge:hover {
    border: 2px solid orangered;
  }

  .github-badge {
    color: var(--primary);
  }
  .github-badge:hover {
    border: 2px solid var(--primary);
  }

  .table-of-contents {
    background-color: var(--surface-100);
    padding: 1.5rem;
    border-radius: var(--radius-medium);
    margin-bottom: 3rem;
  }

  .table-of-contents h2 {
    margin-top: 0;
    color: var(--content);
  }

  .table-of-contents ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 0.5rem;
  }

  .table-of-contents a {
    color: var(--primary);
    text-decoration: none;
    padding: 0.5rem;
    border-radius: var(--radius-small);
    transition: all 0.2s ease;
  }

  .table-of-contents a:hover {
    background-color: var(--surface-200);
    transform: translateX(4px);
  }

  .docs-content {
    line-height: 1.6;
  }

  .docs-section {
    margin-bottom: 4rem;
  }

  .docs-section h2 {
    color: var(--content);
    font-size: 2rem;
    margin-bottom: 1.5rem;
    border-bottom: 2px solid var(--primary);
    padding-bottom: 0.5rem;
  }

  .docs-section h3 {
    color: var(--content);
    font-size: 1.5rem;
    margin: 2rem 0 1rem 0;
  }

  .docs-section h4 {
    color: var(--content);
    font-size: 1.2rem;
    margin: 1.5rem 0 0.5rem 0;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5rem;
    margin: 2rem 0;
  }

  .feature-card {
    background-color: var(--surface-100);
    padding: 1.5rem;
    border-radius: var(--radius-medium);
    text-align: center;
    transition: all 0.3s ease;
  }

  .feature-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
    background-color: var(--surface-200);
  }

  .feature-icon {
    font-size: 2.5rem;
    display: block;
    margin-bottom: 1rem;
  }

  .feature-card h3 {
    margin: 0 0 0.5rem 0;
    color: var(--content);
  }

  .feature-card p {
    margin: 0;
    color: var(--content-200);
  }

  .code-block {
    background-color: var(--surface-100);
    border-radius: var(--radius-medium);
    padding: 1.5rem;
    margin: 1.5rem 0;
    overflow-x: auto;
  }

  .code-block pre {
    margin: 0;
    white-space: pre-wrap;
  }

  .code-block code {
    font-family: var(--font-mono);
    font-size: 0.9rem;
    color: var(--content);
  }

  .warning-box, .info-box {
    padding: 1rem;
    margin: 1.5rem 0;
    border-radius: var(--radius-medium);
    border-left: 4px solid;
  }

  .warning-box {
    background-color: var(--surface-100);
    border-left-color: var(--warning);
  }

  .info-box {
    background-color: var(--surface-100);
    border-left-color: var(--info);
  }

  .warning-box h4, .info-box h4 {
    margin: 0 0 0.5rem 0;
    color: var(--content);
  }

  .api-table {
    margin: 1.5rem 0;
    overflow-x: auto;
  }

  .api-table table {
    width: 100%;
    border-collapse: collapse;
    background-color: var(--surface-100);
    border-radius: var(--radius-medium);
    overflow: hidden;
  }

  .api-table th,
  .api-table td {
    padding: 1rem;
    text-align: left;
    border-bottom: 1px solid var(--surface-200);
  }

  .api-table th {
    background-color: var(--surface-300);
    font-weight: 600;
    color: var(--content);
  }

  .api-table code {
    background-color: var(--surface-300);
    padding: 0.2rem 0.4rem;
    border-radius: var(--radius-small);
    font-size: 0.9rem;
  }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 1.5rem;
    margin: 2rem 0;
  }

  .stat-item {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    padding: 1.5rem;
    background-color: var(--surface-100);
    border-radius: var(--radius-medium);
    transition: all 0.2s ease;
  }

  .stat-item:hover {
    background-color: var(--surface-200);
    transform: translateY(-2px);
  }

  .stat-icon {
    font-size: 2rem;
    flex-shrink: 0;
  }

  .stat-item h4 {
    margin: 0 0 0.5rem 0;
    color: var(--content);
    font-size: 1.1rem;
  }

  .stat-item p {
    margin: 0;
    font-size: 0.9rem;
  }

  .links-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
    margin: 2rem 0;
  }

  .link-card {
    background-color: var(--surface-100);
    padding: 1.5rem;
    border-radius: var(--radius-medium);
    text-decoration: none;
    color: var(--content);
    transition: all 0.3s ease;
  }

  .link-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
    background-color: var(--surface-200);
  }

  .link-card h3 {
    margin: 0 0 0.5rem 0;
    color: var(--primary);
  }

  .link-card p {
    margin: 0;
    color: var(--content);
  }

  .docs-footer {
    text-align: center;
    padding: 2rem;
    border-top: 2px solid var(--surface-200);
    margin-top: 4rem;
    margin-bottom: 4rem;
    color: var(--content);
    background-color: var(--surface-200);
    border-radius: var(--radius-medium);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .docs-container {
      padding: 1rem;
    }

    .docs-header {
      flex-direction: column;
      text-align: center;
    }

    .logo-section {
      flex-direction: column;
      text-align: center;
    }

    .header-text h1 {
      font-size: 2rem;
    }

    .table-of-contents ul {
      grid-template-columns: 1fr;
    }

    .features-grid,
    .stats-grid,
    .links-grid {
      grid-template-columns: 1fr;
    }
  }

  /* Smooth scrolling for anchor links */
  html {
    scroll-behavior: smooth;
  }

  /* Highlight active section */
  .docs-section:target {
    scroll-margin-top: 2rem;
  }
</style>
